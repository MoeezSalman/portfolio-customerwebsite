# Feedback wall shared storage (optional). Without these the API answers
# storage: "local" and each visitor keeps their own posts in the browser.
# Vercel Marketplace > Upstash Redis sets these automatically when connected.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
